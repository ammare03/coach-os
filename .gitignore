# ─────────────────────────────────────────────────────────────
# CoachOS
# See CLAUDE.md §21 — no secrets in the repo, ever.
# ─────────────────────────────────────────────────────────────

# ── Dependencies ──────────────────────────────────────────────
node_modules/
apps/*/node_modules/
packages/*/node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/versions

# ── Environment & secrets ─────────────────────────────────────
# Broad by design. `.env.example` is the ONLY env file that may
# ever be committed, and it must contain placeholders only.
.env
.env.*
!.env.example
*.pem
*.key
*.p8
*.p12
*.jks
*.keystore
*.mobileprovision
*.cer
*.certSigningRequest
google-services.json
GoogleService-Info.plist
service-account*.json
credentials.json
secrets.json

# ── Native folders — regenerated by CNG (CLAUDE.md §25.3) ─────
# Never edit these; never commit them. Config lives in app.config.ts.
/ios/
/android/
apps/*/ios/
apps/*/android/
.kotlin/
*.orig.*

# ── Expo / EAS ────────────────────────────────────────────────
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts
.easignore

# ── Build output & caches ─────────────────────────────────────
build/
.turbo/
.next/
out/
*.tsbuildinfo
.metro-health-check*
.cache/
.parcel-cache/

# ── Tests, coverage, artifacts ────────────────────────────────
coverage/
.nyc_output/
junit.xml
test-results/
playwright-report/
.maestro/tests/**/screenshots/
*.hprof

# ── Logs & crash dumps ────────────────────────────────────────
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
pnpm-debug.*
lerna-debug.*
*.stackdump
bash.exe.stackdump

# ── Local database & scratch ──────────────────────────────────
*.sqlite
*.sqlite3
*.db-journal
/tmp/
scratch/
.scratchpad/

# ── Editor & OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
*.swp
*.swo
*~

# ── Claude Code ───────────────────────────────────────────────
# The planning tree and the skills are deliberately local-only.
# CLAUDE.md and the root specs ARE committed — they are the
# public contract; the plan and skills are the working method.
.claude/plan/
.claude/skills/
.claude/settings.local.json
.claude/projects/
.claude/todos/

# ── Misc ──────────────────────────────────────────────────────
example
