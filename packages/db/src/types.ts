// Inferred row types, re-exported — `$inferSelect` / `$inferInsert` for
// every table (CLAUDE.md §17.1: never hand-write a type Drizzle can infer).
// Empty for now; filled in by
// `phase-01-data-layer/db-package-scaffold/01`.
export {};
