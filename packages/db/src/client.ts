// The server-side connection pool — `postgres(...)` wrapped in
// `drizzle(...)`, exported for apps/api to import. Empty for now; filled in
// by `phase-01-data-layer/db-package-scaffold/01`.
export {};
