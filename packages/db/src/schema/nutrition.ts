// Drizzle tables for the `nutrition` Postgres schema (DATABASE.md DB§5.3) —
// food items, meals, diary entries, and nutrition plans. Empty for now;
// filled in by `phase-01-data-layer/db-package-scaffold/01`.
export {};
