// Drizzle tables for the `platform` Postgres schema (DATABASE.md DB§5.5) —
// webhook_events, audit_log, moderation_actions, export_requests. Empty for
// now; filled in by `phase-01-data-layer/db-package-scaffold/01`.
export {};
