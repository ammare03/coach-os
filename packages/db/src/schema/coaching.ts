// Drizzle tables for the `coaching` Postgres schema (DATABASE.md DB§5.4) —
// media_assets, comments, checkins, messages, live sessions, and the rest
// of the coach↔client interaction tables. Empty for now; filled in by
// `phase-01-data-layer/db-package-scaffold/01`.
export {};
