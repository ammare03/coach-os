// Native Postgres `CREATE TYPE` enums (DATABASE.md DB§4), as Drizzle
// `pgEnum` declarations — user_role, subscription_tier, client_status, and
// the rest. Empty for now; filled in by
// `phase-01-data-layer/db-package-scaffold/01`.
export {};
