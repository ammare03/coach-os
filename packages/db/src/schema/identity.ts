// Drizzle tables for the `identity` Postgres schema (DATABASE.md DB§5.1) —
// users, coach_profiles, client_profiles, invites, and the rest of the
// auth/identity tree. Empty for now; filled in by
// `phase-01-data-layer/db-package-scaffold/01`.
export {};
