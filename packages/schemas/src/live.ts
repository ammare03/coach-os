// Input schemas for `live.*` (createSession, joinToken, end, listUpcoming).
// Filled by phase-19-live-sessions. Empty for now so the module layout is
// visible from error-and-validation/01 onward.
