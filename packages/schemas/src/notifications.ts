// Input schemas for `notifications.*` (list, markRead, registerDevice,
// preferences). Filled by phase-15-notifications. Empty for now so the
// module layout is visible from error-and-validation/01 onward.
