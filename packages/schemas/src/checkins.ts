// Input schemas for `checkins.*` (templates.*, listForClient, submit,
// review). Filled by phase-17-structured-checkins. Empty for now so the
// module layout is visible from error-and-validation/01 onward.
