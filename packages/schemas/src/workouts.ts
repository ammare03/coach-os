// Input schemas for `workouts.*` (upcoming, get, start, logSet, updateSet,
// complete, skip, history). Filled by phase-09-workout-logger. Empty for now
// so the module layout is visible from error-and-validation/01 onward.
