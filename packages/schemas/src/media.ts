// Input schemas for `media.*` (createUploadUrl, confirmUpload, get, delete).
// Filled by phase-11-media-pipeline. Empty for now so the module layout is
// visible from error-and-validation/01 onward.
