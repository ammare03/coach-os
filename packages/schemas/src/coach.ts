// Input schemas for `coach.*`, including its `clients` and `notes`
// sub-routers. Filled by phase-06-onboarding (clients) and
// phase-10-coach-review-surfaces (dashboard, notes). Empty for now so the
// module layout is visible from error-and-validation/01 onward.
