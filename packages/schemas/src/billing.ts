// Input schemas for `billing.*` (entitlements, portalUrl, restore). Filled
// by phase-20-billing-and-entitlements. Empty for now so the module layout
// is visible from error-and-validation/01 onward.
