// Barrel — empty on purpose. `phase-04-design-system` fills this in with
// the shared RN primitives (Button, Card, Sheet, Avatar, …); this task
// (shared-config/04) only creates a correctly-configured, empty package so
// that phase has no setup decision to make.
export {};
