import { Text, View } from 'react-native';

// Placeholder (`phase-05-app-shell/router-skeleton/01`). `navigation-
// primitives` owns the real not-found screen — its copy, its recovery
// action, and its relationship to the forbidden state.
export default function NotFoundScreen() {
  return (
    <View>
      <Text>+not-found</Text>
    </View>
  );
}
