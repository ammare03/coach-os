import { Tabs } from 'expo-router';

// Bare passthrough. `router-skeleton/04` gives this its real configuration —
// Today · Nutrition · Progress · Coach.
export default function ClientTabsLayout() {
  return <Tabs />;
}
