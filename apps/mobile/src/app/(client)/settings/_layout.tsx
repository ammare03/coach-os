import { SettingsStack } from '../../../features/settings/navigation/SettingsStack.tsx';

/** The (coach) counterpart, and the same reasoning. Two route groups, one stack. */
export default function ClientSettingsLayout() {
  return <SettingsStack />;
}
