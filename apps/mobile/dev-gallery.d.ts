// Types for `dev-gallery.js`, which has to stay CommonJS because
// `metro.config.js` requires it from plain Node.
export declare function isDevGalleryEnabled(env?: Record<string, string | undefined>): boolean;
export declare const DEV_GALLERY_OPT_IN: string;
