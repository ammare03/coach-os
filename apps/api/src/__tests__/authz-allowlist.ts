// Placeholder — `authorization-middleware/05-public-allowlist.md` replaces
// this with the real typed structure (`path`, `reason`, `addedBy`,
// `addedOn`) and its staleness/redundancy checks. `04-authz-enumeration-test.md`
// only needs an import target that resolves today; the list itself is not
// this task's concern.
export const PUBLIC_ALLOWLIST: readonly string[] = ['health.ping'];
