import { router } from '../trpc/init.ts';

// Filled by phase-15-notifications. Registered empty now so the tree's
// shape is visible from api-scaffold onward.
export const notificationsRouter = router({});
