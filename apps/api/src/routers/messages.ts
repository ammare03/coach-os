import { router } from '../trpc/init.ts';

// Filled by phase-14-messaging-and-realtime. Registered empty now so the
// tree's shape is visible from api-scaffold onward.
export const messagesRouter = router({});
