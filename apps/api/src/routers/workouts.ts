import { router } from '../trpc/init.ts';

// Filled by phase-09-workout-logger. Registered empty now so the tree's
// shape is visible from api-scaffold onward.
export const workoutsRouter = router({});
