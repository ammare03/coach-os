import { router } from '../trpc/init.ts';

// Filled by phase-12-feedback-comments. Registered empty now so the tree's
// shape is visible from api-scaffold onward.
export const commentsRouter = router({});
