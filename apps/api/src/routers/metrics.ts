import { router } from '../trpc/init.ts';

// Filled by phase-18-habits-metrics-photos. Registered empty now so the
// tree's shape is visible from api-scaffold onward.
export const metricsRouter = router({});
