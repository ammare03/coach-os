import { router } from '../trpc/init.ts';

// Filled by phase-20-billing-and-entitlements. Registered empty now so the
// tree's shape is visible from api-scaffold onward.
export const billingRouter = router({});
