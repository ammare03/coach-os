import { router } from '../trpc/init.ts';

// Filled by phase-17-structured-checkins. Registered empty now so the
// tree's shape is visible from api-scaffold onward.
export const checkinsRouter = router({});
