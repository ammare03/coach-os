import { router } from '../trpc/init.ts';

// Filled by phase-07-exercise-and-program-authoring. Registered empty now
// so the tree's shape is visible from api-scaffold onward.
export const programsRouter = router({});
